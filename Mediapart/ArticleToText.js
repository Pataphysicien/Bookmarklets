console.debug("ArticleToText.js is loaded and being executed.");
(function (undefined) {
    'use strict';
    console.debug("articleToTextJsMain called");

})();

/* Put this code in a bookmark 
    javascript: (function loadFromBookMarklet(undefined) {
        'use strict';
        console.debug('loadFromBookMarklet called');
        var fullJsUri = 'https://raw.githubusercontent.com/Pataphysicien/Bookmarklets/master/Mediapart/ArticleToText.js?v=' + parseInt(Math.random() * 99999999);
        console.debug('loading from address ', fullJsUri);

        var script = document.createElement('script');
        script.type = 'text/javascript';
        script.src  = fullJsUri;
        document.getElementsByTagName('head')[0].appendChild(script);
    })();
*/